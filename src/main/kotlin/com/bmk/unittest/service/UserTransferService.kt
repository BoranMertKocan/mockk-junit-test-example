package com.bmk.unittest.service

import org.springframework.stereotype.Service

@Service
class UserTransferService {

    fun transferUser(firstName: String, lastName: String) {
        // Some transfer process :)
    }
}